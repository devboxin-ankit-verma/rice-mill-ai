"use client";

import { MillVisual } from "./mill-visual";

export function ErpDashboardPremium({ className = "" }: { className?: string }) {
  return (
    <MillVisual
      variant="dashboard"
      className={className}
      alt="Rice mill ERP dashboard on AI control terminal"
    />
  );
}
